$(function() {
  // var $checkedItems = $("form input[type='checkbox']:checked").parents("label")
  // var showItems = $checkedItems.map(function(idx, item) {
  //   return item.textContent.trim();
  // });
  // showItems.each(function(idx, item) {
  //   $("main li:contains(" + item + ")").hide();
  // });})

// click each tag, only those option fit condition show
// User a listener to listen to the event
// check the text. if certain text contained
// 